def randomPrime(upper):
    prime = []
    for num in range(upper + 1):
        if num > 1:
            for i in range(2, num):
                if (num % i) == 0:
                    break
            else:
                prime.append(num)
    import random
    return random.choice(prime)

print(randomPrime(100))